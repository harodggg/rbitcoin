use block;
struct Blockchain { 


}

trait Blockchain { 
    fn default(Block:gensis) -> Blockchian {
        
    }

    fn sync_block(Block:prev_block) -> Blockchain { 

    }

    fn create_block(Block:block) -> bool { 

    }

    fn validator_chain(Block:block) -> bool { 

    }
}